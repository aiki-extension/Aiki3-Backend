services:
  aiki_db:
    image: postgres:15
    container_name: aiki_db
    restart: always
    environment:
      POSTGRES_USER: awesomeuser
      POSTGRES_PASSWORD: secretpw
      POSTGRES_DB: aikidev
    ports:
      - "5433:5432"
    volumes:
      - postgres_data_fastify:/var/lib/postgresql/data

volumes:
  postgres_data_fastify: