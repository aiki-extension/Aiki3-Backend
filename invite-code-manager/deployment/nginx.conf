server {
  listen 80;
  server_name _;

  # Normalize base path without trailing slash.
  location = /ic-manager {
    return 301 /ic-manager/;
  }

  location /ic-manager/ {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /ic-manager/index.html;
  }

  location / {
    return 404;
  }
}
